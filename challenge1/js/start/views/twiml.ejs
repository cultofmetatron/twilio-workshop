<?xml version="1.0" encoding="UTF-8"?>
<Response>
	<% if(digits) { %>
		<% if(digits == '1') { %>
		<!-- 
			TODO:
			Tell the user they are connecting to sales, then forward to another number 
		-->
		<% } else { %>
		<!-- 
			TODO:
			Tell the user they are connecting to services, then forward to another number 
		-->
		<% } %>
	<% } else { %>
	<FIXME>
		<Say>Thanks for calling Acme LLC!</Say>
		<!-- 
			TODO:
			How can you make Twilio use a female voice to <Say> the following text?  
			Use the docs, Luke!
		-->
		<Say>
			For the Sales department, press 1.
			For service, press 2.
		</Say>
	</FIXME>
	<% } %>
</Response>